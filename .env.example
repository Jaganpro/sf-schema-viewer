# ══════════════════════════════════════════════════════════════════════════════
# SF Schema Viewer - Environment Configuration
# ══════════════════════════════════════════════════════════════════════════════
#
# QUICK SETUP: Run ./setup.sh to configure these values automatically!
#
# MANUAL SETUP: Copy this file to .env and fill in the values below.
#
# ══════════════════════════════════════════════════════════════════════════════

# ──────────────────────────────────────────────────────────────────────────────
# Salesforce OAuth Credentials
# ──────────────────────────────────────────────────────────────────────────────
# Get these from your External Client App (recommended) or Connected App:
#
# External Client App (Summer '24+, recommended):
#   Setup → Apps → External Client Apps → External Client App Manager
#
# Connected App (legacy):
#   Setup → Apps → App Manager → New Connected App
#
# Required OAuth Scopes:
#   - Access and manage your data (api)
#   - Perform requests at any time (refresh_token)
#
# Optional Data Cloud Scopes:
#   - Manage Data Cloud profile data (cdp_profile_api)
#   - Query Data Cloud data (cdp_query_api)
#   - Ingest Data Cloud data (cdp_ingest_api)

SF_CLIENT_ID=your_consumer_key_here
SF_CLIENT_SECRET=your_consumer_secret_here

# ──────────────────────────────────────────────────────────────────────────────
# OAuth Callback URL
# ──────────────────────────────────────────────────────────────────────────────
# This must match EXACTLY what you configured in your External Client App.
# Do not change unless you've also updated the Salesforce configuration.

SF_CALLBACK_URL=http://localhost:8000/auth/callback

# ──────────────────────────────────────────────────────────────────────────────
# Session Security
# ──────────────────────────────────────────────────────────────────────────────
# Used to encrypt session cookies. Generate a secure random string:
#   openssl rand -hex 32
#
# The setup.sh wizard generates this automatically.

SESSION_SECRET=change-this-to-a-random-string-in-production

# ──────────────────────────────────────────────────────────────────────────────
# Frontend URL (CORS Configuration)
# ──────────────────────────────────────────────────────────────────────────────
# The URL where the frontend is hosted. Used for CORS and redirects.
# Change to your production URL when deploying.

FRONTEND_URL=http://localhost:5173
