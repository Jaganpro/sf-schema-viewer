# Salesforce Connected App credentials
# Create a Connected App in Salesforce Setup:
# 1. Setup → Apps → App Manager → New Connected App
# 2. Enable OAuth Settings
# 3. Callback URL: http://localhost:8000/auth/callback
# 4. Scopes: Access and manage your data (api), Perform requests at any time (refresh_token)
SF_CLIENT_ID=your_connected_app_consumer_key
SF_CLIENT_SECRET=your_connected_app_consumer_secret
SF_CALLBACK_URL=http://localhost:8000/auth/callback

# Application settings
SESSION_SECRET=change-this-to-a-random-string-in-production
FRONTEND_URL=http://localhost:5173
